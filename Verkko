##Verkko. Generates genome assemblies. --hifi is the imput for Herro-corrected reads, --nano for simplex reads, -d is the output directory. 
verkko -d /*/assembly --local-memory 50 --local-cpus 28 --hifi READS.herro-corrected.fasta --nano READS.fastq


